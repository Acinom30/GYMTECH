import React from 'react';
import UserForm from './userForm';

const RegisterForm = () => {
    return (
        <UserForm initialData={null} isUpdate={false} />
    );
};

export default RegisterForm;